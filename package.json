{
  "name": "webtest",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://tim-fronsee.github.io/",
  "dependencies": {
    "@material-ui/core": "^3.9.2",
    "gh-pages": "^2.0.1",
    "react": "^16.8.2",
    "react-dom": "^16.8.2",
    "react-scripts": "2.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "docs": "rm -rf docs && mkdir docs && cp public/index.html ./docs/",
    "predeploy": "yarn run build && yarn run docs",
    "deploy": "gh-pages -d build",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
